function Header(){
    return(
    <header>
        <strong>NEXUS</strong>

        <nav>
            <a href="#">Games</a>
            <a href="#">Discover</a>
        </nav>

        <button>Sign in</button>
    </header>
    )
}

export default Header